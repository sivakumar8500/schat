import 'package:flutter/material.dart';

class ChatSocketPage extends StatelessWidget {
  const ChatSocketPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('ChatSocket Screen')),
      body: const Center(child: Text('ChatSocket Feature UI')),
    );
  }
}
